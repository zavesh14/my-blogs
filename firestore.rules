rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /siteSettings/aiAssistant {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.email == 'zavesh14@gmail.com';
    }
  }
}
