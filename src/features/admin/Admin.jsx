import { useEffect, useState } from 'react'
import { ArrowUpRight, BookOpen, Camera, Check, FileText, LayoutDashboard, LogOut, Plus, Save, Trash2, UserRound } from 'lucide-react'
import { Link, NavLink, useNavigate } from 'react-router-dom'
import { fetchRealtimeAnalytics } from '../../lib/analytics'
import { auth, signOut } from '../../lib/firebase'
import { gallery, getStored, initialPosts, initialProfile, initialProjects } from '../../lib/storage'

function openResume(resume) {
  if (!resume) return
  if (!resume.startsWith('data:')) { window.open(resume, '_blank', 'noopener,noreferrer'); return }
  const [metadata, encoded] = resume.split(',')
  const mimeType = metadata.match(/data:(.*?);base64/)?.[1] || 'application/pdf'
  const bytes = Uint8Array.from(atob(encoded), (character) => character.charCodeAt(0))
  const url = URL.createObjectURL(new Blob([bytes], { type: mimeType }))
  window.open(url, '_blank', 'noopener,noreferrer')
  window.setTimeout(() => URL.revokeObjectURL(url), 60_000)
}

export default function Admin() {
  const navigate = useNavigate()
  const [posts, setPosts] = useState(() => getStored('wwi-posts', initialPosts))
  const [photos, setPhotos] = useState(() => getStored('wwi-gallery', gallery))
  const [projects, setProjects] = useState(() => getStored('wwi-projects', initialProjects))
  const [profile, setProfile] = useState(() => getStored('wwi-profile', initialProfile))
  const [resume, setResume] = useState(() => localStorage.getItem('wwi-resume') || '')
  const [saved, setSaved] = useState(false)
  const [realtime, setRealtime] = useState({ status: 'loading', activeUsers: 0 })
  useEffect(() => { let active = true; async function load() { try { const data = await fetchRealtimeAnalytics(); if (active) setRealtime({ status: 'ready', ...data }) } catch { if (active) setRealtime({ status: 'error', activeUsers: 0 }) } }; load(); const interval = window.setInterval(load, 30_000); return () => { active = false; window.clearInterval(interval) } }, [])
  const update = (key, setter, items, id, field, value) => { const next = items.map((item) => item.id === id ? { ...item, [field]: value } : item); setter(next); localStorage.setItem(key, JSON.stringify(next)) }
  function addPost() { const next = [{ id: Date.now(), category: 'New note', title: 'A new story waiting to be written', excerpt: 'Start writing your next idea here.', content: 'Write the full story here.', image: '', date: 'Draft', read: '1 min read', color: 'from-slate-500 to-slate-700' }, ...posts]; setPosts(next); localStorage.setItem('wwi-posts', JSON.stringify(next)) }
  function addPhoto() { const next = [...photos, { id: Date.now(), src: '', label: 'New photo' }]; setPhotos(next); localStorage.setItem('wwi-gallery', JSON.stringify(next)) }
  function addProject() { const next = [{ id: Date.now(), category: 'New project', title: 'Untitled project', excerpt: 'Describe what you built and the problem it solves.', date: 'Draft', read: 'View on GitHub', color: 'from-slate-700 to-slate-950', github: '' }, ...projects]; setProjects(next); localStorage.setItem('wwi-projects', JSON.stringify(next)) }
  function saveProfile() { localStorage.setItem('wwi-profile', JSON.stringify(profile)); setSaved(true); setTimeout(() => setSaved(false), 1800) }
  async function logout() { await signOut(auth); sessionStorage.removeItem('wwi-admin'); sessionStorage.removeItem('wwi-admin-email'); navigate('/admin/login') }
  return <div className="admin-shell"><aside className="admin-sidebar"><Link to="/" className="brand"><span className="brand-mark">D</span><span>Deepak<span className="brand-dot">.</span></span></Link><div className="sidebar-label">WORKSPACE</div><NavLink to="/admin" end><LayoutDashboard size={17} /> Overview</NavLink><a href="#posts"><BookOpen size={17} /> Posts <span className="nav-count">{posts.length}</span></a><a href="#gallery"><Camera size={17} /> Gallery <span className="nav-count">{photos.length}</span></a><a href="#projects"><BookOpen size={17} /> Projects <span className="nav-count">{projects.length}</span></a><a href="#profile"><UserRound size={17} /> Profile</a><a href="#resume"><FileText size={17} /> Resume</a><div className="sidebar-bottom"><button onClick={logout}><LogOut size={17} /> Sign out</button></div></aside><div className="admin-content"><div className="admin-topbar"><span className="mobile-admin-title">Deepak Studio / Workspace</span><span className="admin-status"><span className="status-dot" /> Your site is live</span><Link to="/" className="view-site">View site <ArrowUpRight size={14} /></Link></div><div className="admin-main"><div className="admin-heading"><div><p className="eyebrow">SUNDAY, SEPTEMBER 13, 2026</p><h1>Good morning, {profile.name.split(' ')[0]}.</h1><p className="muted">Here’s what’s happening with your site.</p></div><button className="button button-dark" onClick={addPost}><Plus size={16} /> New post</button></div><div className="stats-row"><div><span>Total posts</span><strong>{posts.length}</strong></div><div><span>Gallery photos</span><strong>{photos.length}</strong></div><div><span>Visitors right now</span><strong>{realtime.status === 'ready' ? realtime.activeUsers : '—'}</strong></div>  </div><section className="admin-panel" id="posts"><div className="panel-heading"><h2>Recent posts</h2><button className="subtle-button" onClick={addPost}><Plus size={14} /> Add post</button></div>{posts.map((post) => <div className="admin-post" key={post.id}><div><strong>{post.title}</strong><span>{post.category} · {post.date}</span><label className="admin-label">Full story<textarea value={post.content || post.excerpt} onChange={(e) => update('wwi-posts', setPosts, posts, post.id, 'content', e.target.value)} rows="4" /></label></div></div>)}</section><section className="admin-panel" id="profile"><div className="panel-heading"><h2>Edit profile</h2><button className="save-button" onClick={saveProfile}>{saved ? <Check size={15} /> : <Save size={15} />} {saved ? 'Saved' : 'Save profile'}</button></div><label className="admin-label">Name<input value={profile.name} onChange={(e) => setProfile({ ...profile, name: e.target.value })} /></label><label className="admin-label">Role / tagline<input value={profile.role} onChange={(e) => setProfile({ ...profile, role: e.target.value })} /></label><label className="admin-label">Bio<textarea value={profile.bio} onChange={(e) => setProfile({ ...profile, bio: e.target.value })} rows="4" /></label></section><section className="admin-panel gallery-panel" id="gallery"><div className="panel-heading"><h2>Edit gallery</h2><button className="subtle-button" onClick={addPhoto}><Plus size={14} /> Add photo</button></div>{photos.map((photo) => <div className="gallery-admin-card" key={photo.id}><label className="admin-label">Caption<input value={photo.label} onChange={(e) => update('wwi-gallery', setPhotos, photos, photo.id, 'label', e.target.value)} /></label><button className="delete-button" onClick={() => { const next = photos.filter((item) => item.id !== photo.id); setPhotos(next); localStorage.setItem('wwi-gallery', JSON.stringify(next)) }}><Trash2 size={14} /> Delete</button></div>)}</section><section className="admin-panel project-admin-panel" id="projects"><div className="panel-heading"><h2>Projects</h2><button className="subtle-button" onClick={addProject}><Plus size={14} /> Add project</button></div>{projects.map((project) => <div className="project-admin-card" key={project.id}><label className="admin-label">Title<input value={project.title} onChange={(e) => update('wwi-projects', setProjects, projects, project.id, 'title', e.target.value)} /></label><label className="admin-label">Description<textarea value={project.excerpt} onChange={(e) => update('wwi-projects', setProjects, projects, project.id, 'excerpt', e.target.value)} /></label></div>)}</section><section className="admin-panel resume-panel" id="resume"><div className="panel-heading"><h2>My resume</h2><span>{resume ? 'Uploaded' : 'Not uploaded'}</span></div><div className="resume-actions"><label className="button button-dark upload-button"><FileText size={16} /> Choose PDF<input type="file" accept="application/pdf,.pdf" onChange={(event) => { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => { const value = String(reader.result); setResume(value); localStorage.setItem('wwi-resume', value) }; reader.readAsDataURL(file) }} /></label>{resume && <button className="subtle-button" onClick={() => openResume(resume)}>Open resume</button>}</div></section></div></div></div>
}
